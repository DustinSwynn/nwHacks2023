import { MediaControllerState } from '@livepeer/core';
import * as React from 'react';
import { PlaybackDisplayErrorType } from '../PlaybackDisplayErrorType';
export type ControlsContainerProps = {
    loadingText?: string | null;
    showLoadingSpinner?: boolean;
    hidePosterOnPlayed?: boolean;
    poster?: React.ReactNode;
    playbackDisplayErrorType?: PlaybackDisplayErrorType | null;
    top?: React.ReactNode;
    middle?: React.ReactNode;
    left?: React.ReactNode;
    right?: React.ReactNode;
};
type ControlsContainerStateSlice = Pick<MediaControllerState, 'togglePlay' | 'canPlay' | 'buffered'>;
type ControlsContainerCoreProps = ControlsContainerStateSlice & ControlsContainerProps;
export declare const useControlsContainer: (props: ControlsContainerCoreProps) => {
    isLoaded: boolean;
    containerProps: {
        onPress: () => void;
    };
};
export {};
