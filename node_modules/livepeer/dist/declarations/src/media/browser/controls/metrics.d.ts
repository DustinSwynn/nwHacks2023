import { MediaControllerStore, MediaMetrics, MetricsStatus, PlaybackMonitor } from '@livepeer/core/media';
/**
 * Gather playback metrics from a generic HTML5 video/audio element and
 * report them to a websocket. Automatically handles a redirect to get the
 * metrics endpoint.
 *
 * @param element Element to capture playback metrics from
 */
export declare function addMediaMetricsToInitializedStore<TElement extends HTMLMediaElement>(store: MediaControllerStore<TElement> | undefined | null, sourceUrl: string | undefined | null, onError?: (error: unknown) => void): MediaMetrics<TElement>;
export type { MediaMetrics, MetricsStatus, PlaybackMonitor };
